.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Web UI bundle: built by build_web.py and linked into the firmware image
# (see include/WebAssets.h). Both are regenerated from web_src/ on every build.
web_src/node_modules
web_src/dist
src/generated/

# Per-developer IDE + tool state
.idea/

# Claude workflow, not contributor docs — the build facts a contributor needs are
# in the README's "Building from source". Excluded from the published repo.
.claude/
CLAUDE.md
